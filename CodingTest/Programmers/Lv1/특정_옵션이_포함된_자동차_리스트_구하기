-- 코드를 입력하세요
SELECT 
    CR.CAR_ID AS CAR_ID,
    CR.CAR_TYPE AS CAR_TYPE,
    CR.DAILY_FEE AS DAILY_FEE,
    CR.OPTIONS AS OPTIONS
FROM CAR_RENTAL_COMPANY_CAR CR
WHERE CR.OPTIONS LIKE '%네비게이션%' ORDER BY CR.CAR_ID DESC;
